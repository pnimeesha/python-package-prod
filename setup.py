from setuptools import setup, find_packages

setup(
    name = 'packaging-demo',
    version = '0.0.0',
    packages = find_packages(),
    include_package_data = True,
    
    author = 'my_name',
    install_requires = [
        
    ]
        
)